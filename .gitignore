# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
newbreed_logo.png
node_modules
dist
dist-ssr
*.local
.gear/
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Large generated Bible data (build-time normalized)
data/web.canonical.json

# Whisper model + binary (user-provided, large)
/models/
/bin/

# Toolchain installers downloaded into the project while setting a machine up
# (the Vulkan SDK is ~275 MB and lands here by default).
/*.exe
/whisper.cpp/

# All generated Bible data (downloaded, large, public-domain)
data/*.canonical.json

# Python cache
scripts/__pycache__/
__pycache__/

# Screenshots
Screenshot*.png
*.screenshot.png

# Personal-tier songs (copyrighted, personal use only, never committed)
data/personal.songs.json
personal-songs.txt
data/songs-source/

# Per-flavor build installers
installers/

# Per-flavor bundling staging dir (model + whisper binary copied in per build)
bundled/

# Sermon recordings used to test the detector (private, not ours to distribute)
sermons/

# Learning/measurement run artifacts + private sermon transcripts + seed snapshots
learning-results/
/whisper-cublas-*/
/src-tauri/.backends.tauri.conf.json
